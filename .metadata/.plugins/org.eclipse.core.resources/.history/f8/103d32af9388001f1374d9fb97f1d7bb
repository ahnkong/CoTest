package level1;

import java.util.Arrays;

public class d241012_K번째수_정렬 {
	public static void main(String[] args) {

		int[] array = { 1, 5, 2, 6, 3, 7, 4 };
		int[][] commands = { { 2, 5, 3 }, { 4, 4, 1 }, { 1, 7, 3 } };
		int[] answer = new int[commands.length];

		
//		System.out.println(commands[0][0]);//2
//		System.out.println(commands[0][1]); //5
//		System.out.println(commands[0][2]);//3
//		
//		
//		System.out.println(commands[1][0]);//4
//		System.out.println(commands[1][1]); //4
//		System.out.println(commands[1][2]);//1
		
        // 각 명령에 대해 처리
        for (int i = 0; i < commands.length; i++) {
            int start = commands[i][0];  // 시작 인덱스 (1-based index)
            int end = commands[i][1];    // 끝 인덱스 (1-based index)
            int kNum = commands[i][2];   // K번째 수

            // 자를 배열의 크기를 계산하여 동적으로 subArray 생성
            int[] subArray = new int[end - start + 1];

            // array 배열에서 start-1부터 end까지 값을 subArray로 복사
            for (int j = 0; j < subArray.length; j++) {
                subArray[j] = array[start - 1 + j];
                System.out.println("i는(" +i + ")" + "+ j는(" + j + ")" + subArray[j]);
            }
            System.out.println();
            // 자른 배열을 정렬
            
            System.out.println("소팅 전 : " + Arrays.toString(subArray));
            Arrays.sort(subArray);
            System.out.println("소팅 후 : " + Arrays.toString(subArray));

            // K번째 수 찾기 (kNum은 1-based index이므로 kNum - 1)
            answer[i] = subArray[kNum - 1];
        }

        // 결과 출력
        System.out.println(Arrays.toString(answer));
    }
}

//for (int i = 0; i < cut.length; i++) {
//	for (int j = 0; j < cut[i].length; j++) {
//		cut[i][j] = 0;
//		System.out.print(cut[i][j] +" ");
//	}
//	System.out.println();
//}
//
//
//for (int i = 0; i < commands.length; i++) {
//	first = commands[i][0];
//	System.out.println(i + "번째 - " + first);
//	for (int j = 0; j <= array.length; j++) {
//		cut[i][0] += array[j+first]+i; 
//		System.out.println(Arrays.deepToString(cut));
//	}
////	cut[i] = 
//			
//}	
//
//System.out.println("-----------------");	
//	
//for (int i = 0; i < commands.length; i++) {
//	second = commands[i][1];
//	System.out.println(i + "번째 - " + second);
//}

