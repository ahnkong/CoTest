package level2;

public class d241003_푸드파이터 {
	public static void main(String[] args) {
		int[] food = {1, 3, 4, 6};
		String answer = ""; // 문자열은 오는 순서대로 붙으니까!
		
		int[] numFood = new int[food.length];
		//이걸 합해줄거야! right + water + left이렇게 가져오면 되는거
		// 문자열의 합 말고, 배열을 하나 더 만들어줘서, arraylist로 만들고,
		// 
		//배열을 문자열로 꺼내올때, 짝수로만 가져올 수 있다.
		// 조건 1) 
//		for(int count : food) {
//			numFood+=count;
//			System.out.println("총 음식 갯수 : " + numFood);
//			
//		}
		String right = "";
		String water = "";
		String left = "";
		
		int temp = 0;
		
		for (int i = 0; i < food.length; i++) {
			if(food[i]/2 < 0 ) {
				break;
			}else if(food[i]/2 >0 ) {
				numFood[i] = food[i]/2;
				System.out.println(i +"가 일때, temp는 "+ temp + "이다");
			
			}
			
			
			
		}
		

		
		
//		
//		
//		
		
	}
}
